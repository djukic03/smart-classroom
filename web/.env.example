# Base URL of the FastAPI backend. Leave empty to use the dev proxy at /api.
VITE_API_BASE_URL=

# Backend origin the Vite dev server proxies /api and /health to.
VITE_DEV_PROXY_TARGET=http://localhost:8000
